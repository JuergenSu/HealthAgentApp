parameters:
  app.openai.api_key: '%env(OPENAI_API_KEY)%'
  app.openai.model: '%env(OPENAI_MODEL)%'

services:
  _defaults:
    autowire: true
    autoconfigure: true
  App\:
    resource: '../src/'
  App\OpenAI\OpenAiClient: '@App\OpenAI\UnavailableOpenAiClient'
